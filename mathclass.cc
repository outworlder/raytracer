#include "mathclass.hpp"

/*
void Matrix::LoadIdentity(void) {
   for (int i=0; i<max; i++)
     for (int j=0; j<max; j++)
       data[i][j]=(i==j) ? 1.0f : 0.0f;
}

void Matrix::Transpose(void) {
   float temp;
   for (int i=0; i<max; i++)
     for (int j=0; j<max; j++) {
	temp==data[j][i];
	data[j][i]=data[i][j];
	data[i][j]=temp;
     }
}   

Matrix::Matrix() {       
   LoadIdentity();
}
*/


